/* eslint-disable no-multi-spaces */

/**
 * 接口 定义 (全是post的)
 */

const serverApiLibrary = {
  // miniProgramLogin: 'auth/miniProgramLogin',             // 目前为临时接口，每次调用接口得到一个固定token，可暂时用于调用其余接口。
  // qryItemConfig: 'hospital/qryItemConfig',               // 获取医院前端配置
  // qryMessageConfig: 'message/qryAllMessageConfig',       // 查询消息提示
  
  qryList: 'list'

};

export default serverApiLibrary;
